[build]
  command = "pnpm install && pnpm prod-build"
  publish = "dist"

# Redirects for SPA support
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
